import styled from "styled-components";
import { Header } from "../components/Header";

const Root = styled.div``;

const LandingPage = () => {
  return (
    <Root>
      <Header />
    </Root>
  );
};

export { LandingPage };
