export enum IssueStateFilter {
  open = "open",
  closed = "closed",
  all = "all",
}

export enum IssueSortingOption {
  comments = "comments",
  created = "created",
  updated = "updated",
}

export enum IssueSortingDirection {
  asc = "asc",
  desc = "desc",
}
